using System;
using UnityEngine;

public abstract class AStrategy : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public abstract void Execute();
    
}
